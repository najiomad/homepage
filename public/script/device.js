$(function() {
    let winW = $(window).width();
    const devW = 769;
    if (winW >= 1281) {
        // 1281px以上
    } else if (winW >= 769) {
        // 1281px未満
    } else {
        // 769px未満
    }
});